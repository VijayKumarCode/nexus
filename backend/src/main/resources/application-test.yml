spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: "" # Safe: Isolated internal JVM memory loop
  jpa:
    hibernate:
      ddl-auto: create-drop
app:
  jwt-secret: "test_secure_environment_signing_key_must_be_long_and_robust_32_bytes_minimum"