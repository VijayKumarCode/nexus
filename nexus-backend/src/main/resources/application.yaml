app:
  base-url: https://unawed-paislee-noncontrollablely.ngrok-free.app

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/nexus_db}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:nexus123}
    driver-class-name: org.postgresql.Driver

  mail:
    host: smtp.gmail.com
    port: 587
    username: vkumar.kumar31@gmail.com
    password: bukdxlcujhumphki
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            trust: smtp.gmail.com

        debug: true


  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none # 🔥 SAFE DEFAULT
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect